BONES                                                                               -*- Org -*-


* TODO [#B] test "smart-spill" branch
* TODO Write manual
  - document: usage, caveats, differences to the standard, extensions
    to the standard, embedding, building, register-usage, using
    $inline/$primitive, code-generation strategy, optimizations
    performed, GC, data-representation, interesting projects (in-core
    compilation, emitting elf-modules directly, etc.), porting guide,
    performance guide.
* TODO "eqv?" does structural compare, not excluding pairs, strings or vectors
  - somewhat weakly defined by depending on distinct storage locations.
  - alternatively: return true if headers are the same, size == 1 and the 1st slots are equal
    (this shoudld cover flonums and chars).
* TODO test integer-only string<->number conversion
* TODO unboxing
  - "($the TYPE EXP)" annotates with type.
  - "let" can check for "$the" and assign FP-register.
  - accessing variables in FP-registers needs to move them to normal registers.
  - requires special intrinsics for FP arguments.
  - implement primitives as "($typed-case-lambda (((VAR TYPE) ...) BODY ...) ...)"
    - specially handled in operator position during canonicalization.
  - have local FA pass ("lfa") that expands $typed-case-lambda according to inferred
    types and convert "let"-bound flonum vars using a custom binding form
    - e.g. "($let-float ...)" or something like this.
* TODO Consider port to Windows
  - use mingw64.
* TODO [#B] try compiling grond
* TODO move code to bitbucket
* TODO something is wrong with musl's "sprintf" (or varargs) implementation
  - "(number->string (expt 2.1 3))" returns "nan".
  - ok with glibc.
* TODO implement basic known value optimization
  - beta-contraction (needs knowledge about FVs).
  - direct-calls (implies loops).
  - perhaps a bit of constant-propagation.
* TODO try using libgc as 2nd heap generation
  - needs write-barrier to record assignments of heap-data into non-heap objects.
    - then evict recursively.
  - also, keep GC-count in block header.
* TODO pre-allocated chars in the range 0-255
  - means: all chars are preallocated, unless we allow chars with arbitrary code.
* TODO "(if X S1 S2)" can be computed more efficiently
  - use "cmovCC" if S1, S2 are simple-expressions.
* TODO write file:README
  - introduction
  - build-instructions
* TODO think about windows port
  - does program reside in low memory? check file:/archive/optimizing_assembly.pdf
* TODO try "default rel" (RIP-relative addressing)
  - seems to work without problems.
  - no noticable difference in speed, but slightly smaller.
* TODO add partial R7RS support
  - modulo libraries.
    - or can we parse "define-library" manually and expand it into some
      alexpander magic?
  - needed: parameters, numerical stuff, copying primitives,
    bytevectors.
* TODO code for "asin"
  - http://forums.codeguru.com/showthread.php?262979-How-to-compute-asin
    fld   st(0)             ;copy sine value
    fmul  st,st(0)          ;sin^2
    fld1
    fsubr                   ;1-sin^2 = cos^2
    fsqrt                   ;->cos
    fpatan                  ;i.e. arctan(sin/cos) = arcsin
  - more
    Function ArcSin(X As Double) As Double
	ArcSin = Atn(X / Sqr(-X * X + 1))
    End Function

    Function ArcCos(X As Double) As Double
	ArcCos = Atn(-X / Sqr(-X * X + 1)) + 2 * Atn(1)
    End Function

    Private Sub Command1_Click()
	' replace the 0.5 below with the degree (in radians) that
	' you want to calculate its ArcSin or ArcCos value
	MsgBox "ArcSin(0.5) = " & ArcSin(0.5)
	MsgBox "ArcCos(0.5) = " & ArcCos(0.5)
    End Sub
* TODO log: "fld1; fld XXX; fyl2x"
  - seems to be broken.
* TODO remove unused globals
* TODO secure inline-primitives against redefinition of internally used primitives
  - like this:
    (let-syntax ((PRIMITIVE PRIMITIVE))
      (define-inline (OTHER-PRIMITIVE ...) ...))
  - or redefinition of PRIMITIVE will change behaviour of OTHER-PRIMITIVE.
* R5RS support
** TODO missing
   asin acos
** TODO broken
   log
** "string->number" does not support exactness or base prefixes
** Unimplemented
   numerator, denominator
   make-rectangular, make-polar, real-part, imag-part, magnitude, angle
   rationalize
   complex?
   load, eval
   transcript-on, transcript-off
   interaction-environment, scheme-report-environment, null-environment
   char-ready?
* TODO write detailed list of differences to R5RS
  - no type or range checks.
  - no overflow-detection.
  - max/min do not change exactness (well, not always - identify cases).
  - primitives are not necessarily "eq?"
  - "set!" of primitive might not necessarily work ("define" is ok, though).
  - 3rd arg to "substring" is optional.
  - behaviour of transcendental functions with regard to nan/inf may be wrong.
  - and more
** Non-standard extensions
*** Alexpander extensions
    - generalized syntax-rules
    - splicing let[rec]-syntax
    - nested syntax-binding forms.
    - single-arg define
    - SRFI-46
    - non-syntax-rules as 2nd arg to define-syntax
*** Primitives
   emergency-exit
   current-process-id
   current-error-port
   current-directory
   read-string
   eof-object
   void
   nan?
   port?
   return-to-host
   bitwise-and bitwise-ior bitwise-xor bitwise-not arithmetic-shift
   finite?
   system
   exit
   open-input-string open-output-string get-output-string
   open-append-output-file
   write-string
   reclaim
   current-second
   get-environment-variable
   file-exists?
   delete-file
   define-syntax-rule
   define-values
   free
   cut
   when unless
   fluid-let
   optional let-optionals
   begin0
   assert
   cond-expand
   call/cc
   current-jiffy
   jiffies-per-second
   case-sensitive
   command-line
   error
   print
* TODO mark places in file:x86_64/boneslib.s for 32-bit alignment issues
  - flonum allocation
    - "ALLOCATE_FLONUM"
    - "$allocate", perhaps align bytevectors in general.
    - "alloc_block"
  - GC
    - mark: insert alignment marker when copying bytevector block.
    - scan: skip alignment marker.
* Issues
** "inexact->exact" uses "fisttp", so needs SSE3
** i don't fully trust that "integer?" handles all cases correctly
   - is the exponent overflow-test correct?
** align flonums so that block-data is at 16-byte boundary
   - this is supposed to be faster.
   - but needs to be handled in GC (skip padding bytes.)
   - also needed for 32-bit variant with flonum support
     - when allocating flonums, align ALLOC first.
     - GC: when copying, insert marker word before flonum; during scan,
       check for marker word and skip.
* Referencess
  - X86_64:
    - FPU instructions: http://www.pmzone.org/appendixF.html
    - FPU tutorial: http://www.website.masmforum.com/tutorials/fptute/fpuchap10.htm
    - x86 reference: http://www.fermimn.gov.it/linux/quarta/x86/index.htm
    - SSE: http://softpixel.com/~cwright/programming/simd/sse.php
    - General information: file:/archive/optimizing_assembly.pdf
    - x86-64 ABI: file:/archive/x86-64-abi.pdf
  - MIPS
    - http://en.wikipedia.org/wiki/MIPS_instruction_set
    - Instructions, SPIM-desc. is better, though: http://www.mrc.uidaho.edu/mrc/people/jff/digital/MIPSir.html
    - Datasheet, somewhat compressed: file:/archive/4KcProgMan.pdf
    - ABI: file:/archive/mipsabi32.pdf
    - SPIM MIPS description: http://www.doc.ic.ac.uk/lab/secondyear/spim/node9.html
  - RA:
    http://pages.cs.wisc.edu/~cs701-1/NOTES/5.REGISTER-ALLOCATION.html
* Notes
** the write barrier can be disabled by defining DISABLE_WRITE_BARRIER
** enable GC logging with -DENABLE_GC_LOGGING
